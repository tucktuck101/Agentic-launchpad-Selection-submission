# Requirements for scripts/install-requirements.sh
#
# System packages are installed with apt on Debian/Ubuntu systems.
# Node app dependencies are installed from each app's package.json.

[apt]
git
python3
docker.io
nodejs
npm

[npm]
challenge_two/launchpad-code-review-app/code-review-app
challenge_two/failing-tests-app

[python]
# The provided API server uses only the Python standard library.
challenge_two/launchpad-api-script-server/api-script-server
